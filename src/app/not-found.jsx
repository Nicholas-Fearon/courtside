// export a component in a file called not-found
// the component has to be called 'NotFound'

export default function NotFound() {
    return (
        <div>
            <h2>Oh no, something went wrong!</h2>
            <p>I could not find that page! Sorryyyyy</p>
        </div>
    )
}